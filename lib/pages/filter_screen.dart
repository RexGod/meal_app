import 'package:flutter/material.dart';

class FiltersScreen extends StatelessWidget {
  static const routName = '/Filter';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('filters here'),
    );
  }
}
